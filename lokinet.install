post_install() {
  # Previous AUR layouts used a symlink /etc/loki/lokinet.ini -> /var/lib.
  # Pacman sysusers/tmpfiles hooks own user and directory creation.
  if [[ -L /etc/loki/lokinet.ini ]]; then
    rm -f /etc/loki/lokinet.ini
  fi
}

post_upgrade() { post_install; }
